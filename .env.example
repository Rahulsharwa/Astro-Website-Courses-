# Web3Forms access key — makes the counselling form deliver enquiries by email.
# Get a free key at https://web3forms.com (enter your email, they send the key).
# Copy this file to `.env` and paste the key below, then rebuild.
# Without it the form tells visitors to call instead of silently losing the enquiry.
PUBLIC_WEB3FORMS_KEY=
